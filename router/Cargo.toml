[package]
name = "snow-weather-router"
version = "0.2.0"
edition = "2021"
publish = false

[lib]
name = "SnowWeatherRouter"
crate-type = ["cdylib"]

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
strip = true
